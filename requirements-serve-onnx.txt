# Requirements for ONNX serving container
# Used by docker/serve-onnx.Dockerfile
# Minimal dependencies for fast, lightweight inference

# ONNX inference
onnxruntime>=1.16.0
numpy>=1.24.0,<2.0.0

# Web serving
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# GCP integration
google-cloud-storage>=2.10.0

# Monitoring (optional)
prometheus-client>=0.19.0
